# Contributing

Use small pull requests with tests or eval coverage for behavior changes.

Before opening a PR:

```bash
npm run ci:local
```

Agent changes should describe the affected tool contract, guardrail, or eval scenario.
